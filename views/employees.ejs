<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">
    <title>Employees</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: rgba(240, 248, 255, 0.8); /* Light transparent background */
        }
        nav {
            background-color: rgba(173, 216, 230, 0.5); /* Light blue with transparency */
            padding: 10px 0;
        }
        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            overflow: hidden;
            text-align: center;
        }
        nav ul li {
            display: inline;
            margin: 0 15px;
        }
        nav ul li a {
            color: #4682b4; /* Steel blue */
            text-decoration: none;
            font-weight: bold;
        }
        nav ul li a:hover {
            color: #5f9ea0; /* Cadet blue on hover */
        }
        h1 {
            text-align: center;
            color: #4682b4; /* Steel blue */
            margin-top: 20px;
        }
        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            background-color: rgba(255, 255, 255, 0.6); /* White with light transparency */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        table, th, td {
            border: 1px solid rgba(70, 130, 180, 0.5); /* Light steel blue border */
        }
        th, td {
            padding: 10px;
            text-align: center;
        }
        th {
            background-color: rgba(173, 216, 230, 0.7); /* Light blue with transparency */
            color: #4682b4; /* Steel blue text */
        }
        td {
            background-color: rgba(255, 255, 255, 0.7); /* White with light transparency */
        }
        td a {
            color: #4682b4; /* Steel blue links */
            text-decoration: none;
        }
        td a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <nav>
        <ul>
            <li><a href="/register">Home</a></li>
            <li><a href="/employees">Employees</a></li>
            <li><a href="/add_transactions">Transaction</a></li>
            <li><a href="/history">History</a></li>
            <li><a href="/attendance">Attendance</a></li>
            <li><a href="/">Signout</a></li>
          
        </ul>
    </nav>
    
    <h1><i class="fa-solid fa-users"></i>Employee List</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Department</th>
                <th>Designation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% employees.forEach(employee => { %>
                <tr>
                    <td><%= employee.id %></td>
                    <td><%= employee.name %></td>
                    <td><%= employee.department %></td>
                    <td><%= employee.designation %></td>
                    <td>
                        <a href="/employees/<%= employee.id %>/view">View</a> |
                        <a href="/employees/<%= employee.id %>/delete">Delete</a>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>
